#include "libft.h"

int	ft_tco_int(int yes, int yea, int nah)
{
	if (yes)
		return (yea);
	else
		return (nah);
}

void	*ft_tco_voidStr(int yes, void *yea, void *nah)
{
	if (yes)
		return (yea);
	else
		return (nah);
}
